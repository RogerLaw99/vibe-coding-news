---
description: 新建或从零搭建项目时，优先初始化 Git 仓库
alwaysApply: true
---

# 项目先初始化 Git

在为本工作区新建项目、脚手架或大量新文件时，**第一步**应完成 Git 初始化：

1. 若根目录尚无 `.git`，执行 `git init`
2. 确保存在合理的 `.gitignore`（至少忽略 `node_modules/`、`dist/`、`.env*`、日志等）
3. 在首次有意义的功能/结构就绪后，创建初始提交（用户未要求时不要额外 commit）

不要跳过 Git 初始化直接堆代码；除非用户明确说不要版本控制。
